<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.itcast.mapper.ReviewMapper">
	<select id="selectReview" resultType="cn.itcast.pojo.ReviewPojo">
		select * 
		from review order by num ;
	</select>
</mapper>