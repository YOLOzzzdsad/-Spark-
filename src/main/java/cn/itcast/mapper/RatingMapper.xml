<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.itcast.mapper.RatingMapper">
	<select id="selectRating" resultType="cn.itcast.pojo.RatingPojo">
		select * 
		from rating ORDER BY num;
	</select>
</mapper>